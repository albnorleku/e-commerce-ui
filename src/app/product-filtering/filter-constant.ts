/**
 * Filter constant for slider threshhold.
 *
 *  RAM ( > 1024 when  capacity is greater than 1 TB)
 *  Cores (> 64 when  capacity is greater than 64 Core)
 */
export const FilterConstant = {
    RAM: 1024,
    CORES: 64
};
